version: "3.9"

services:
  backend:
    volumes:
      - ./backend:/var/www/html
    command: >
      sh -c "composer install &&
             apache2-foreground"

  frontend:
    volumes:
      - ./frontend/public:/usr/local/apache2/htdocs
